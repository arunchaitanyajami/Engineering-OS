{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capability set for the Engineering OS desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
